<%= render partial: "home/home_form", 
        locals: {type: "user",
                 photos_feeds: @photos_feeds,
                 albums_feeds: @albums_feeds,
                 photos_discover: @photos_discover,
                 albums_discover: @albums_discover} %>