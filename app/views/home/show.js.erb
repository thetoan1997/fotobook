$("#photos_feeds").html("<%= escape_javascript(render partial: 'photos_feeds', :locals => {photos_feeds: @photos_feeds}) %>");
$("#albums_feeds").html("<%= escape_javascript(render partial: 'albums_feeds', :locals => {albums_feeds: @albums_feeds}) %>");
$("#photos_discover").html("<%= escape_javascript(render partial: 'photos_discover', :locals => {photos_discover: @photos_discover}) %>");
$("#albums_discover").html("<%= escape_javascript(render partial: 'albums_discover', :locals => {albums_discover: @albums_discover}) %>");

